[project]
name = "tools"
version = "1.3.0"
description = "Camada operacional canônica do ecossistema preditivo: runners, locks, provenance, redaction, manifests. Stdlib-only por design — nenhuma dependência externa."
requires-python = ">=3.13"
dependencies = []
